{
  "private": true,
  "scripts": {
    "dev": "cross-env NODE_OPTIONS='-r @newrelic/next' next",
    "build": "next build",
    "start": "cross-env NODE_OPTIONS='-r @newrelic/next' next start",
    "lint": "next lint"
  },
  "dependencies": {
    "next": "latest",
    "react": "18.2.0",
    "react-dom": "18.2.0"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "cross-env": "^7.0.3"
  }
}
